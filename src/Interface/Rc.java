package Interface;

public class Rc {
// it is like a class but we cannot Create the object for the interface

    // but we can directly call the Function
    public static void main(String[] args) {
    System.out.println(a.a);
}
    interface a{
        int a=10;
    }
}
